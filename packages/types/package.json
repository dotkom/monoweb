{
  "name": "@dotkomonline/types",
  "main": "./src/index.ts",
  "exports": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src --format esm --dts --clean --sourcemap",
    "lint": "eslint --max-warnings 0 .",
    "lint:fix": "run -T eslint --fix ."
  },
  "dependencies": {
    "zod": "^3.19.1"
  },
  "devDependencies": {
    "@types/eslint": "^8",
    "eslint": "^8.28.0",
    "tsup": "^6.5.0"
  }
}
