import { z } from 'zod';

export const JobListingLocationLinkScalarFieldEnumSchema = z.enum(['id','createdAt','jobListingId','locationId']);

export default JobListingLocationLinkScalarFieldEnumSchema;
