import type { Theme } from "theme-ui";

const checkboxTheme: Theme = {
  forms: {
    checkbox: {
      borderColor: "blue",
      "&:hover": {
        cursor: "pointer",
      },
    },
  },
};

export default checkboxTheme;
