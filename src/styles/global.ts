import { ThemeStyles } from "theme-ui";

const globalStyles: ThemeStyles["root"] = {
  "html, body, #__next": {
    width: "100%",
    minHeight: "100vh",
  },
  "#__next": {
    bg: "#FBFCFD",
  },
};

export default globalStyles;
