{
  "name": "mailer",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/lambda.ts --format=cjs --bundle --outfile=dist/lambda.js",
    "lint": "run -T eslint --max-warnings 0 .",
    "lint:fix": "run -T eslint --fix .",
    "test": "run -T jest",
    "dev": "node --loader ts-node/esm --experimental-specifier-resolution=node src/server.ts"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.95",
    "aws-sdk": "^2.1123.0",
    "express": "4.17.3",
    "marked": "^4.0.15",
    "zod": "3.14.3"
  },
  "devDependencies": {
    "@dotkom/eslint-config-ow": "workspace:*",
    "@types/express": "4",
    "@types/marked": "^4.0.3",
    "esbuild": "^0.14.38",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  }
}
