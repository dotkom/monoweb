import { InvalidDeadlineError } from "@dotkomonline/core"

throw new InvalidDeadlineError("hello")
// // Start the REPL
// const replServer = repl.start({
//   prompt: '> ',
// });

// // Attach your module to the REPL context
// replServer.context.core = core;
